# Nonlinear Dependence

This example follows the R correlation/dependence vignette pattern: compare a
linear relationship with a nonlinear relationship.

Run:

```bash
uv run python docs/examples/dependence.py
```

The script demonstrates:

- `nns_dep`
- `nns_cor`
- why dependence can remain strong when linear correlation is less informative
