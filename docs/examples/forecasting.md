# Forecasting

This example is a compact Python version of the R forecasting vignette. It uses
a deterministic synthetic seasonal series so the script has no data dependency.

Run:

```bash
uv run python docs/examples/forecasting.py
```

The script demonstrates:

- `nns_arma`
- `nns_arma_optim`
- `nns_var`
