# Distributions And ANOVA

This example is a compact Python version of the R distribution-comparison
vignette. It compares two groups and computes a partial-moment CDF.

Run:

```bash
uv run python docs/examples/distributions_anova.py
```

The script demonstrates:

- `nns_cdf`
- `nns_anova`
- pairwise distribution comparison output
